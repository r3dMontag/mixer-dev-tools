<form  action="/auth" method="post">
  <% scope_groups = @display_scopes.each_slice((@display_scopes.size/4.0).ceil).to_a %>
  <% scope_groups.each do |g| %>
    <div class="col-md-3">
      <% g.each do |s| %>
        <div title="<%= s['description'] %>" class="checkbox">
          <label>
            <input type="checkbox" name="selected_scopes[]" value="<%= s['scope'] %>"><%= s['scope'] %>
          </label>
        </div>
      <% end %>
    </div>
  <% end %>
  <div class="form-group">
    <div class="text-center">
      <button type="submit" class="btn btn-primary btn-lg">Get token</button>
    </div>
  </div>
</form>
